{
  "name": "obviouslee",
  "private": true,
  "workspaces": ["shared", "game"],
  "scripts": {
    "dev:game": "npm run dev --workspace=game",
    "dev:creator": "npm run dev --workspace=creator",
    "build:game": "npm run build --workspace=game",
    "build:creator": "npm run build --workspace=creator"
  }
}
